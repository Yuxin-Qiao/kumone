name: Legacy Upstream Release Placeholder

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    name: No-op placeholder
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Downstream releases are handled by release-downstream.yml"
